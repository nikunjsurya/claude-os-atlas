# claude OS — atlas

A constellation-style visual atlas of every project, skill, agent, reference, and instruction in my Claude + AI ecosystem. Auto-derived from `~/MEMORY.md`, `~/.claude/`, `~/Projects/`, and `~/_templates/`. Localhost-only, read-only V1. Mission-control execute layer arrives in V2.

See `docs/superpowers/specs/` for the design spec.
